{
  "name": "anam-app-email-server",
  "version": "1.0.0",
  "description": "Serverless function to send emails for ANAM-APP.",
  "main": "api/send-notifications.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.1.1",
    "nodemailer": "^6.9.13",
    "resend": "^3.2.0"
  }
}